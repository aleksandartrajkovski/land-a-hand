import React from 'react';

const SearchBar = () => {

    return (
        <input
            type="text"
            placeholder="Search here!"
            className="search-input"
        />
    );
};

export default SearchBar;
